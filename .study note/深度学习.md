# 深度学习

## 神经网络

### 感知机

### 激活函数

### 多维数组的运算

### 3层神经网络的实现

### 输出层设计

## 神经网络的学习

### 损失函数

### 梯度

### 学习算法的实现

## 误差反向传播法

计算图

链式法则

反向传播

简单层

激活层

Affine/Softmax层

误差反向传播的实现

## 与学习相关的技巧

参数的更新

权重的初始值

Batch

正则化

超参数的验证

## 卷积神经网络

卷积层的完整计算可以写为：

$$
z = \sum_i x_i w_i + b
$$

BN（Batch Normalization）在推理阶段可以表示为：

$$
\operatorname{BN}(z)
= \gamma \frac{z-\mu}{\sqrt{\sigma^2+\epsilon}} + \beta
$$

其中，$\mu$、$\sigma^2$ 是训练得到的均值和方差，$\gamma$、$\beta$ 是可学习参数。

ReLU 激活函数为：

$$
\operatorname{ReLU}(z) = \max(0,z)
$$

Leaky ReLU 激活函数为：

$$
\operatorname{LeakyReLU}(z)
=
\begin{cases}
z, & z \ge 0 \\
\alpha z, & z < 0
\end{cases}
$$

在 FPGA 推理加速中，BN 经常可以和卷积参数融合，减少额外计算。融合后的形式为：

$$
y = a z + c
$$

其中：

$$
a = \frac{\gamma}{\sqrt{\sigma^2+\epsilon}}
$$

$$
c = \beta - \frac{\gamma \mu}{\sqrt{\sigma^2+\epsilon}}
$$

因此，原来的卷积 + BN 可以等价改写为新的权重和偏置：

$$
W' = aW
$$

$$
b' = ab + c
$$

这样可以在硬件中减少 BN 模块的独立计算开销，提高推理效率。

